name: version2
description: A high-fidelity Flutter conversion.
publish_to: 'none'

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter

  go_router: ^13.2.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  lucide_icons: ^0.257.0
  google_fonts: ^6.1.0
  animate_do: ^3.3.4
  flutter_riverpod: ^2.4.9
  http: ^1.2.0
  firebase_core: ^4.7.0
  cloud_firestore: ^6.3.0
  firebase_auth: ^6.4.0
  flutter_map: ^8.3.0
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
